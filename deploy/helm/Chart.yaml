apiVersion: v2
name: beta-spray
description: Beta Spray deployment chart
type: application
version: "1.0.0"
appVersion: "{{ .Values.version_sha }}"
dependencies:
  - name: ingress-nginx
    repository: https://kubernetes.github.io/ingress-nginx
    version: "4.0.18"
