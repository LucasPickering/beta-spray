/**
 * The different types of things we can drag around using React DnD
 *
 * TODO make this an ADT to convey the drop result type
 */
export enum DragType {
  BetaMoveSvg = "betaMoveSvg",
  BetaMoveList = "betaMoveList",
}
